# URL base do backend para desenvolvimento local (host, SEM /api).
# Os serviços já incluem o prefixo /api nos caminhos.
# A aplicação não sobe sem esta variável definida (validada em src/config/env.ts).
VITE_API_BASE_URL=http://localhost:8080/api
